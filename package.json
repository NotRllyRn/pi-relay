{
  "name": "pi-relay",
  "version": "0.1.0",
  "description": "Multi-account OpenAI Codex quota relay for Pi",
  "type": "module",
  "license": "MIT",
  "keywords": ["pi-package"],
  "files": ["index.ts", "src", "README.md", "LICENSE"],
  "pi": { "extensions": ["./index.ts"] },
  "engines": { "node": ">=22" },
  "scripts": {
    "test": "node --import tsx --test test/*.test.ts",
    "typecheck": "tsc --noEmit",
    "check": "npm run typecheck && npm test"
  },
  "peerDependencies": {
    "@earendil-works/pi-ai": "0.83.0",
    "@earendil-works/pi-coding-agent": "0.83.0"
  },
  "devDependencies": {
    "@earendil-works/pi-ai": "0.83.0",
    "@earendil-works/pi-coding-agent": "0.83.0",
    "@types/node": "^22.15.0",
    "tsx": "^4.20.0",
    "typescript": "^5.8.0"
  }
}
